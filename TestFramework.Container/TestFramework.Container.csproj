<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net8.0;net10.0</TargetFrameworks>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<IsTestProject>false</IsTestProject>
		<Title>TestFramework.Container</Title>
		<Version>0.1.0</Version>
		<Authors>DeadMoon0</Authors>
		<Product>TestFramework</Product>
		<Description>Shared Docker building blocks for TestFramework container environments: declaring where an application comes from and building it, network creation, host and network endpoint addressing, readiness waits and container log capture.</Description>
		<PackageProjectUrl>https://github.com/DeadMoon0/TestFramework-Container</PackageProjectUrl>
		<PackageIcon>Icon.png</PackageIcon>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<RepositoryUrl>https://github.com/DeadMoon0/TestFramework-Container.git</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageTags>Test;UnitTest;IntegrationTest;Framework;Timeline;Docker</PackageTags>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<Copyright>Copyright (c) DeadMoon0 2026</Copyright>
		<PackageReleaseNotes>Initial release: shared Docker building blocks, and a declared source model for getting an application into a container.
Worth knowing up front:
- Where a container's application comes from is declared, not guessed: Image(...), Project(...) - resolved against the file that declares it - or Directory(...). Project facts come from MSBuild rather than from inspecting paths, a multi-targeted project is a hard error instead of a silent pick, and the whole resolved plan is logged before anything is built, so a failure can be read against what was going to happen.
- Published ports bind to 127.0.0.1, not to every interface. A test SQL Server or emulator is no longer reachable from the rest of the network while a suite runs. Set TESTFRAMEWORK_CONTAINER_HOST_IP when the Docker daemon is remote and the loopback default would make the port unreachable.
- Docker host detection runs before the first connection. Docker Desktop on Windows exposes the engine under more than one named pipe, and picking the wrong one produced a connection failure on a machine where Docker was working fine.
- Images and temporary build output this package creates are labelled and swept when they are more than a day old, because a killed test host leaves them behind and the container reaper does not cover them. Set TESTFRAMEWORK_CONTAINER_NO_SWEEP to disable.
- Docker CLI invocations are bounded by a timeout, so a wedged CLI fails teardown instead of hanging it forever.</PackageReleaseNotes>
	</PropertyGroup>

	<ItemGroup>
		<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
			<_Parameter1>TestFramework.Container.Tests</_Parameter1>
		</AssemblyAttribute>
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Data.SqlClient" Version="6.1.2" />
		<!-- Direct reference overrides the vulnerable SSH.NET 2025.1.0 that Testcontainers pulls in
		     transitively (GHSA-q939-rpr3-3284). Testcontainers still pins it as of 4.13.0. -->
		<PackageReference Include="SSH.NET" Version="2026.0.0" />
		<PackageReference Include="Testcontainers" Version="4.11.0" />
		<PackageReference Include="Testcontainers.MsSql" Version="4.11.0" />
		<PackageReference Include="TestFramework.Core" Version="0.1.10" />
	</ItemGroup>

	<ItemGroup>
		<None Include="..\Modules\TestFramework-Common\Assets\Icon.png">
			<Pack>True</Pack>
			<PackagePath>\</PackagePath>
		</None>
		<None Include="README.md">
			<Pack>True</Pack>
			<PackagePath>\</PackagePath>
		</None>
	</ItemGroup>

</Project>
